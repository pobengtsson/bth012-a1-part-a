/**  add your tests to this file and add test files as you
 * see necessary to make your code structure readable and
 * testable */

import { kalle } from '../src/index'

describe('will this work', () => {
  it('just might', () => {
    expect(kalle()).toEqual(1)
  })
})
